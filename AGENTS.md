# AGENTS.md — wechat-memory-agent 协作约定

## 需求异议规则（最高优先级）

如果用户提出的需求存在**逻辑错误或根本性问题**，或者**明显偏离原有需求**时，必须：

1. **首先提醒用户**，指出具体问题所在，不要直接照做；
2. 若确认自己的判断更优，**坚持最优解并给出明确理由**；
3. **只有当用户明确坚持原需求时，才放行执行**（此时按用户要求实现，但保留异议记录在对话中）。

> 目的：避免"用户说什么就做什么"导致的错误实现扩散到代码库。沉默的顺从不是好的协作。

示例场景：
- 需求会破坏已有设计原则（如"原文永不改动"、raw/extracted 分离）时 → 提醒并给替代方案
- 需求与 DESIGN.md 已确认的决策冲突时 → 指出冲突点，请用户裁决
- 需求包含技术上不可能或有更优路径的假设时 → 先纠偏再动手

## 项目背景速览

- 设计文档：`docs/DESIGN.md`（所有架构决策的唯一权威来源，改动需同步更新）
- 回归测试：`tests/`（89 例）。**每次修复 bug 或搜索出错，先加测试用例再修代码**；提交前 `.venv/Scripts/python -m pytest tests/` 必须全绿
- 提交规范：单次提交聚焦一个主题（feat/fix），推送前确认 `.env` 未被跟踪（含密钥）
- 启动方式：`scripts/run.py` 是唯一入口（无密钥→扫码；有密钥→直连）
